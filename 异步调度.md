## Scheduler
### Stage 1 加队列
    add_request()
    新请求加入 waiting 队列
### Stage 2 调度
    schedule()
#### Stage 2.1 先调度decode
    首先遍历所有请求，并且不超过单步跑的token上限
    while req_index < len(self.running) and token_budget > 0:
    然后计算新的token数
    num_new_tokens = request.num_tokens_with_spec - request.num_computed_tokens
    num_tokens_with_spec = 已确认的token数 + 投机解码猜测的token数
    num_new_tokens = min(num_new_tokens, token_budget)
    
    然后分配kv cache block
    new_blocks = self.kv_cache_manager.allocate_slots(request, num_new_tokens) #再议
    
    kv cache不够就抢占优先级最低的请求
    pop # 为什么不能原来的先算完？因为先来的先遍历，本质上还是先来先服务
    _preempt_request(preempted_req) # 释放kv cache block，num_computed_tokens 置零 放回waiting
#### Stage 2.2 后调度waiting
    while (self.waiting or self.skipped_waiting) and token_budget > 0:
    if len(self.running) == self.max_num_running_reqs: # 如果达到最大并发，就不调度
        break

    _select_waiting_queue_for_scheduling 一般来说（FCFS 模式），先调度 skipped_waiting，因为它们之前已经检查过一次
    request = request_queue.peek_request() # 获取第一个请求

    # 查看阻塞请求
    if self._is_blocked_waiting_status(request.status) and not self._try_promote_blocked_waiting_request(request):
    # 流式输入有无完成，PD分离下kv cache有没有被推过来
        request_queue.pop_request()
        step_skipped_waiting.prepend_request(request)  # 放入本轮跳过队列
        continue

    # 分配kv cache
    if request.num_computed_tokens == 0:
    new_computed_blocks, num_new_local_computed_tokens = \
        self.kv_cache_manager.get_computed_blocks(request)  # 前缀缓存匹配
    new_blocks = self.kv_cache_manager.allocate_slots(
    request, num_new_tokens,
    full_sequence_must_fit=self.scheduler_reserve_full_isl,)
    if new_blocks is None:
        break  # KV cache 不够，停止调度新请求
#### Stage 2.3 waiting -> running
    request = request_queue.pop_request()
    self.running.append(request)
    request.status = RequestStatus.RUNNING
    request.num_computed_tokens = num_computed_tokens
#### Stage 2.4 将本轮跳过的请求放回 skipped_waiting
    if step_skipped_waiting:
        self.skipped_waiting.prepend_requests(step_skipped_waiting)
### Stage 3 处理执行结果（forward结束）
    for req_id, num_tokens_scheduled in num_scheduled_tokens.items():
        request = self.requests.get(req_id)
        
        # 1. 获取生成的 token
        generated_token_ids = sampled_token_ids[req_index]
        
        # 2. 检查是否停止
        new_token_ids, stopped = self._update_request_with_output(request, new_token_ids)
        
        # 3. 如果停止
        if stopped:
            finished = self._handle_stopped_request(request)
            if finished:
                kv_transfer_params = self._free_request(request)  # 释放资源
    
    
